#using the divmod function for division
#first variable=quotient
#second variable=reminder
quotient,reminder=divmod(99,5)
print(quotient,reminder)
print(quotient)
print(reminder)
#the variables can be declared at any names